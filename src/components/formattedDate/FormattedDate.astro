---

interface Props {
  date: Date;
}

const { date } = Astro.props;

console.log(date.toISOString())

---

<time class="mt-6 text-sm text-zinc-400">

  {
    date.toLocaleDateString("es-CL", {
      year: "2-digit",
      month: "short",
      day: "numeric",
    })
  }
</time>
